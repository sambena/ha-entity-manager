__pycache__/
*.pyc

# Local config override (real host/paths) -- config.example.json is the tracked template
config.json

# Runtime data: snapshots of removed entities, local to this machine
data/

# Access token saved locally by start.ps1, if you opt in
.token
